"use client";
import { FormEvent, useState } from "react";
const base=(process.env.NEXT_PUBLIC_API_BASE_URL??"").replace(/\/$/,""); const api=(p:string)=>`${base}${p}`;
type Order={code:string;buyerName:string;whatsapp:string;totalCents:number;status:string;receiptUploaded:boolean;guests:{id:number;name:string;kind:string}[]};
export default function AdminPage(){const [token,setToken]=useState("");const [password,setPassword]=useState("");const [orders,setOrders]=useState<Order[]>([]);const [error,setError]=useState("");const load=async(t=token)=>{const r=await fetch(api('/api/admin'),{headers:{Authorization:`Bearer ${t}`}});const d=await r.json();if(!r.ok)throw Error(d.error);setOrders(d.orders)};const login=async(e:FormEvent)=>{e.preventDefault();try{const r=await fetch(api('/api/admin'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const d=await r.json();if(!r.ok)throw Error(d.error);setToken(d.token);await load(d.token)}catch(x){setError(x instanceof Error?x.message:'Falha')}};const action=async(code:string,action:'approve'|'reject')=>{await fetch(api('/api/admin'),{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({code,action})});await load()};return <main className="admin-page">{!token?<form className="card" onSubmit={login}><p className="kicker">PVT ALQUIMISTA</p><h1>Administração</h1><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" required/><button className="btn" type="submit">Entrar</button>{error&&<p>{error}</p>}</form>:<section><header><p className="kicker">ADMIN</p><h1>Pedidos e comprovantes</h1></header>{orders.map(o=><article className="card" key={o.code}><b>{o.code} · R$ {(o.totalCents/100).toFixed(2)}</b><p>{o.buyerName} · {o.whatsapp}</p><p>{o.guests.map(g=>g.name).join(', ')}</p><p>Status: {o.status}</p>{o.status==='pending_approval'&&<div><button className="btn" onClick={()=>action(o.code,'approve')}>Aprovar</button><button className="btn" onClick={()=>action(o.code,'reject')}>Recusar</button></div>}</article>)}</section>}</main>}
